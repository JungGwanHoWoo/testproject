using System;
using System.Collections.Generic;
using System.Linq;
using System.Runtime.InteropServices.Swift;
using System.Runtime.Intrinsics.Arm;
using System.Text;
using System.Threading.Tasks;

namespace LikeLion
{
    class Program
    {
        static void Main(string[] args)
        {
            // //사용자 입력을 문자열 받기
            // System.Console.Write("이름을 입력하세요: ");
            // string userName = Console.ReadLine();

            // System.Console.WriteLine($"안녕하세요, {userName}님!");

            // //문자열을 정수로 변환
            // System.Console.Write("나이를 입력하세요: ");
            // string input = Console.ReadLine(); //사용자로부터 입력받기
            // int age = int.Parse(input); // 문자열을 정수로 변환

            // System.Console.WriteLine($"내년에는 {age+1} 살이 되겠군요!"); //변화된 값 사용

            //해당 스샷내용
            //루인 스킬 피해 : 4.5 <- 입력받기

            // char persent = '%';
            // System.Console.Write("루인 스킬 피해를 입력하세요: ");
            // float 루인스킬피해 = float.Parse(Console.ReadLine());

            // System.Console.Write("\n카드 게이지 획득량를 입력하세요: ");
            // float 카드게이지획득량 = float.Parse(Console.ReadLine());

            // System.Console.Write("\n각성기 피해 를 입력하세요: ");
            // float 각성기피해 = float.Parse(Console.ReadLine());

            // System.Console.Write("\n최대마나를 입력하세요: ");
            // int 최대마나 = int.Parse(Console.ReadLine());

            // System.Console.Write("\n전투 중 마나 회복량를 입력하세요: ");
            // int 전투중마나회복량 = int.Parse(Console.ReadLine());

            // System.Console.Write("\n비전투 중 마나 회복량를 입력하세요: ");
            // int 비전투중마나회복량 = int.Parse(Console.ReadLine());

            // System.Console.Write("\n이동속도를 입력하세요: ");
            // float 이동속도 = float.Parse(Console.ReadLine());

            // System.Console.Write("\n탈것속도를 입력하세요: ");
            // float 탈것속도 = float.Parse(Console.ReadLine());

            // System.Console.Write("\n운반속도를 입력하세요: ");
            // float 운반속도 = float.Parse(Console.ReadLine());

            // System.Console.Write("\n스킬재사용대기시간감소를 입력하세요: ");
            // float 스킬재사용대기시간감소 = float.Parse(Console.ReadLine());

            // System.Console.WriteLine($"\n루인 스킬 피해 : {루인스킬피해}{persent}");
            // System.Console.WriteLine($"\n카드 게이지 획득량 : {카드게이지획득량}{persent}");
            // System.Console.WriteLine($"\n각성기 피해 : {각성기피해}{persent}");
            // System.Console.WriteLine($"\n최대 마나 : {최대마나}");
            // System.Console.WriteLine($"\n전투중 마나 회복량 : {전투중마나회복량}");
            // System.Console.WriteLine($"\n비전투 중 마나 회복량 : {비전투중마나회복량}");
            // System.Console.WriteLine($"\n이동속도 : {이동속도}{persent}");
            // System.Console.WriteLine($"\n탈것속도 : {탈것속도}{persent}");
            // System.Console.WriteLine($"\n운반속도 : {운반속도}{persent}");
            // System.Console.WriteLine($"\n스킬 재사용 대기시간 감소 : {스킬재사용대기시간감소}{persent}");
            

        }

    }

}